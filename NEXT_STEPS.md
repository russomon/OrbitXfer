# Next Steps

Repo: OrbitXfer

Use this file for the short forward-looking queue, not the full project history.

## Now

- Use OrbitXfer, not `orelay`, for future peer-to-peer transfer work.
- Confirm the second computer can clone or pull `russomon/OrbitXfer`, run the CLI build, and run the Tauri GUI build.
- Decide whether to archive the obsolete `orelay` GitHub repo and remove its local checkout after reviewing any unique untracked notes.

## Soon

- Keep release docs and tags synchronized whenever a releasable change lands.
- Verify Tauri macOS signing/notarization setup before the next packaged release.
- Refresh any cross-machine setup notes after the second computer has been tested.

## Later

- Consider adding a small health-check script that verifies Rust, Node, Electron, and the bundled CLI path for a fresh machine.

## Blockers

- None recorded.
